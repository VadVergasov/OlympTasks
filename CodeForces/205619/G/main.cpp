#include <bits/stdc++.h>

using namespace std;

int main() {
    ifstream in("input.txt");
    ofstream out("output.txt");
    long long n, m;
    in >> n >> m;
    long long last;
    in >> last;
    while (in >> last) {
        continue;
    }
    vector<long long> res = {last};
    for (int i = 0; i < res.size(); i++) {
        long long cur = res.size();
    }
    return 0;
}