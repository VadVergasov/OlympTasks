#include <bits/stdc++.h>

using namespace std;

int main() {
    long long t;
    cin >> t;
    for (int q = 0; q < t; q++) {
        long long n;
        cin >> n;
        vector<long long> a(n);
        for (auto &i : a) {
            cin >> i;
        }
    }
    return 0;
}