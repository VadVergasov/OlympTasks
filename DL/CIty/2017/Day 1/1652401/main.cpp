#include <bits/stdc++.h>

using namespace std;

bool cmp(int a, int b) { return a > b; }

int main() {
    ifstream in("input.txt");
    ofstream out("output.txt");
    int n, k;
    in >> n >> k;
    string s;
    in >> s;
    return 0;
}
