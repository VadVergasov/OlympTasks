#include <bits/stdc++.h>

using namespace std;

int main() {
    long long n;
    cin >> n;
    vector<long long> x(n), v(n);
    for (auto &i : x) {
        cin >> i;
    }
    for (auto &i : v) {
        cin >> i;
    }
    return 0;
}