#include <bits/stdc++.h>

using namespace std;

int main() {
    vector<int> test(1e6);
    cin >> test[1231];
    // int n, m, q;
    // cin >> n >> m >> q;
    // vector<vector<char>> a(n + 2, vector<char>(m + 2, '#'));
    // for (int i = 1; i <= n; i++) {
    //     for (int j = 1; j <= m; j++) {
    //         cin >> a[i][j];
    //     }
    // }
    // string s;
    // cin >> s;
    // while (q--) {
    //     int x, y, z;
    //     cin >> x >> y >> z;
    //     x--;
    //     y--;
    //     z--;
    // }
    return 0;
}