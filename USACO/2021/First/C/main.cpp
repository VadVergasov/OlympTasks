#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    map<int, pair<int, int> > x, y;
    for (auto &i : a) {
        char c;
        cin >> c;
        int x, y;
        cin >> x >> y;
    }
    return 0;
}
