#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, s, tmp;
    cin>>n>>s;
    vector<int> a;
    for(int i=0;i<n;i++){
        cin>>tmp;
        a.push_back(tmp);
    }
    return 0;
}
