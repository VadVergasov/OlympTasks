#include <bits/stdc++.h>

using namespace std;

int main() {
    long long n, a, b, w, h;
    cin >> n >> a >> b >> w >> h;
    long long l = 0, r = 1e18;
    while (l <= r) {
        long long m =
    }
    return 0;
}