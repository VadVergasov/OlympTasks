// https://codeforces.com/group/kubgwEbWlB/contest/268895/problem/B
#include <bits/stdc++.h>

using namespace std;

int main() {
    long long n, m;
    cin >> n >> m;
    vector<long long> a(m);
    for (auto &i : a) {
        cin >> i;
    }
    long long res = 0, num = 1;
    sort(a.begin(), a.end());
    do {
        int n1 = n;

    } while (next_permutation(a.begin(), a.end()));
    return 0;
}