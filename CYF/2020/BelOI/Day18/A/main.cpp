#include <bits/stdc++.h>

using namespace std;

int main() {
    string p, t;
    cin >> t >> p;
    vector<int> a(t.size());
    for (auto &i : a) {
        cin >> i;
        i--;
    }
    return 0;
}