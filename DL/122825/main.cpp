#include <bits/stdc++.h>

using namespace std;

int main(){
    ifstream in("input.txt");
    ofstream out("output.txt");
    int n, res=-10e9;
    in>>n;
    vector<int> a(n);
    for(int i=0;i<n;i++){
        in>>a[i];
    }
    for(int k=2;k<=n/2;k++){

    }
    return 0;
}
